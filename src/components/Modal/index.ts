import { registerComponent } from '../../framework/componentRegister';
import Modal from './modal.tmpl';

registerComponent(Modal);

export default Modal;
export type { ModalField } from '../../entities/Modal';
